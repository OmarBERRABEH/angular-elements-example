# Angular Elements Example

This project is a first try of using the experimental `@angular/elements`. 

## Install

```
yarn
```

## Build

Run `yarn start` to build the project. The build artifacts will be stored in the `dist/` directory.

